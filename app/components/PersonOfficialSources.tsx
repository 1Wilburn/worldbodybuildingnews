// trigger deploy type Person={name:string;official_url?:string;instagram?:string;x_twitter?:string;youtube?:string;federation?:string;federation_url?:string;nationality?:string;};function norm(u?:string){if(!u)return null;const s=u.trim();if(s.startsWith('http'))return s;if(s.startsWith('@'))return 'https://instagram.com/'+s[1:];return s;}export default function PersonOfficialSources({person}:{person:Person}){const rows:any[]=[];if(person.official_url)rows.push({label:'Official Website',href:person.official_url});const ig=norm(person.instagram),tw=norm(person.x_twitter),yt=norm(person.youtube);if(ig)rows.push({label:'Instagram',href:ig});if(tw)rows.push({label:'X (Twitter)',href:tw});if(yt)rows.push({label:'YouTube',href:yt});if(person.federation||person.federation_url)rows.push({label:person.federation?`${person.federation} (Official)`:'Official Federation',href:person.federation_url});return(<aside className='rounded-xl border bg-white p-4'><h3 className='text-lg font-semibold mb-3'>Official Sources</h3>{rows.length?<ul className='space-y-2'>{rows.map((r,i)=>(<li key={i}>{r.href?<a href={r.href} className='text-blue-700 underline break-all'>{r.label}</a>:<span>{r.label}</span>}</li>))}</ul>:<p className='text-neutral-600 text-sm'>No official links on file yet.</p>}{person.nationality&&<div className='mt-4 text-sm text-neutral-700'><span className='font-medium'>Nationality: </span>{person.nationality}</div>}</aside>);}
